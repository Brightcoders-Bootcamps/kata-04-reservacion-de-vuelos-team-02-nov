import React from 'react';
import {Text} from 'react-native';
import {styles} from '../../../styles/SignUpStyles';

const SignUpTitle = () => {
  return <Text style={styles.title}>Sign Up</Text>;
};

export default SignUpTitle;
