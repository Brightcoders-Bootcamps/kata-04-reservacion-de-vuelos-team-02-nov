import React, {useEffect} from 'react';
import {Text, Button, TextInput} from 'react-native';
import {styles} from '../../styles/SignUpStyles';

const SignUpTitle = () => {
  return <Text style={styles.title}>Sing Up</Text>;
};

export default SignUpTitle;
